---
const { project } = Astro.props;
---
<a href={project.url} class="card block">
  <div class="aspect-video rounded-xl bg-black/50 mb-3 grid place-items-center">
    <span class="text-white/60">Preview</span>
  </div>
  <h3 class="font-display text-xl">{project.title}</h3>
  <p class="text-white/70 mt-2">{project.description}</p>
  <p class="text-white/50 text-sm mt-3">{project.stack.join(" · ")}</p>
</a>
